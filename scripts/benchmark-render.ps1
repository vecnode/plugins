# Offline render benchmark placeholder — extend with headless plugin host when available.
param(
  [string]$Plugin = "CamelotSynth"
)

Write-Host "Benchmark placeholder for $Plugin."
Write-Host "Next step: add headless ProcessBlock loop at 48 kHz / 128 frames and report max block time."
exit 0
